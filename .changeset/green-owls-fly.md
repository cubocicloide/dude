---
"@cubocicloide/dude": minor
"@cubocicloide/stack-react-fastapi": minor
---

feat: generated project includes pinned package.json + .npmrc — `dude init` now writes a root `package.json` with `@cubocicloide/dude` pinned to the exact version used at init time, and a `.npmrc` ready for GitHub Packages auth
