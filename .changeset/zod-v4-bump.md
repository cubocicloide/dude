---
"@cubocicloide/dude": patch
---

Bump `zod` dependency from `^3` to `^4`. Our usage of `z.object`, `z.union`, `z.discriminatedUnion`, `.parse`, `.safeParse`, and `ZodError.issues` is unchanged between v3 and v4.
