---
'@cubocicloide/stack-react-fastapi': patch
---

Document `dude info` and `dude report` in the generated project's "Working with dude" docs page, so a bug in dude itself (rather than the app) has a clear, discoverable path to a well-formed report.
