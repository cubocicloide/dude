# Changesets

This folder is used by [changesets](https://github.com/changesets/changesets) to track release notes for each package in the monorepo.

To add a changeset, run `make changeset` from the repo root and follow the prompts.
