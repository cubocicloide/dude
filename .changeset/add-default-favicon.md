---
'@cubocicloide/stack-react-fastapi': patch
---

Add a default favicon to the base template (`frontend/public/favicon.svg`, wired into `index.html`) so scaffolded projects don't ship with a blank browser tab icon. It's a simple placeholder — swap it for your own.
