# npmjs.com automation token used by `make promote` / `make dist-tags` /
# `make release` (publish rights on the @cubocicloide scope). Optional — if
# unset, those targets fall back to your existing npm auth (`npm login`).
NPM_TOKEN=
