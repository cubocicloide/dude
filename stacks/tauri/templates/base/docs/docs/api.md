# Command reference

This page is generated from the live command catalog — run `dude docs` and it
is refreshed automatically, or regenerate it manually:

```bash
dude help --format md > docs/docs/api.md
```
