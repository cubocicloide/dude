---
paths:
  - "src/components/**"
---

# FE001 — Component directory naming

Every direct child of `src/components/` must be PascalCase: `^[A-Z][a-zA-Z0-9]+$`.

| Valid       | Invalid      |
| ----------- | ------------ |
| `UserCard/` | `userCard/`  |
| `NoteList/` | `note-list/` |
