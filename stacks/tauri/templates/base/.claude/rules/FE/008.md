---
paths:
  - "src/**"
---

# FE008 — Static assets location

Every static asset (images, fonts, media: `.svg`, `.png`, `.woff2`, …) must
live in `src/assets/`. No asset files anywhere else under `src/`.

Import them from there:

```tsx
import logo from '@/assets/logo.svg'
```
