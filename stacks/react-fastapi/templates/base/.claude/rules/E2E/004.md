---
paths:
  - "e2e/pages/**"
---

# ET004 — Page object naming

Page object files must follow the `*Page.ts` convention (PascalCase + `Page` suffix). The class name must match the filename.

| Valid | Invalid |
|-------|---------|
| `LoginPage.ts` | `login-page.ts` |
| `TodoDetailPage.ts` | `todoDetail.ts` |

One class per file.

```typescript
// e2e/pages/LoginPage.ts
export class LoginPage {
  constructor(private readonly page: Page) {}
  // ...
}
```
