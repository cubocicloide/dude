---
paths:
  - "backend/app/queries/**"
---

# BE011 — Query class naming

Every class in `backend/app/queries/foo.py` must be named with the PascalCase prefix derived from the filename. Multiple classes per file are allowed (one per operation is the norm).

| File | Valid class names |
|------|------------------|
| `queries/todos.py` | `Todos`, `TodosList`, `TodosCreate`, `TodosById`, … |
| `queries/users.py` | `Users`, `UsersMe`, `UsersDelete`, … |

A file with no classes at all is an error.
