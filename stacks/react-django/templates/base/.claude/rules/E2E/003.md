---
paths:
  - "e2e/steps/**"
---

# ET003 — Step file → feature pairing (no orphans)

Every `*.steps.ts` file must have a matching `.feature` file. Exception: `common.steps.ts` is exempt (it holds shared steps).

An orphaned step file without a corresponding feature is an error.
