---
paths:
  - "frontend/src/components/**"
---

# FE002 — Allowed files inside a component directory

A component directory may contain **only**:

| Item | Required? |
|------|-----------|
| `index.tsx` | **Mandatory** |
| `styles.module.css` | Optional |
| `types.tsx` | Optional |
| `components/` | Optional — sub-components (same rules apply recursively) |

Any other file or subdirectory is an error.

```
UserCard/
  index.tsx
  styles.module.css
  components/
    Avatar/
      index.tsx

UserCard/
  index.tsx
  utils.ts          ← FE002 error
  UserCard.test.ts  ← FE002 error
```
