---
paths:
  - "frontend/src/hooks/**"
---

# FE006 — Hook directory naming and contents

Hook directories must match `^use[A-Z][a-zA-Z0-9]*$`.

| Valid | Invalid |
|-------|---------|
| `useAttachments/` | `useattachments/` |
| `useTodoList/` | `todo-list/` |

A hook directory may contain **only**:

| Item | Required? |
|------|-----------|
| `index.tsx` | **Mandatory** |
| `types.tsx` | Optional |

No subdirectories, no other files. Shared logic belongs in `frontend/src/utils/`.
