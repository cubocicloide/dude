# Dependencies
node_modules/
.pnpm-store/

# Build output
dist/
build/
# Allow the `build` command folder inside iac providers (named after the dude command, not a build artifact)
!stacks/react-fastapi/src/commands/iac/providers/*/commands/build/
# Allow the tauri stack's `build` command folder (named after the dude command, not a build artifact)
!stacks/tauri/src/commands/build/
*.tsbuildinfo

# Turbo
.turbo/

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Env
.env
.env.local
.env.*.local

# OS
.DS_Store
Thumbs.db

# Editors
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json

# Coverage
coverage/
*.lcov

# Cache
.cache/
.eslintcache

# Local testing — not tracked by git
private/

.mcp.json